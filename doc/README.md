# Documentation index

This folder contains project documentation files. The primary MongoDB developer guide is:

- `doc/mongo.md` — consolidated MongoDB developer guide (installation, docker usage, mongosh queries, helper scripts)

Other docs may be added here as the project grows. Use this file as a quick index for documentation under `doc/`.

